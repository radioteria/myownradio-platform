
<!doctype html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <link href='http://fonts.googleapis.com/css?family=Fira+Sans:300,400&subset=cyrillic,latin' rel='stylesheet' type='text/css'>
    <title>{{ error.code }} - {{ error.message }}</title>
    <style>
        body {
            background-color: #ffffff;
        }
        p {
            font-family: 'Fira Sans', sans-serif;
            font-weight: 300;
            font-size: 48px;
            line-height: 60px;
            color: #333333;
            text-align: center;
        }
        .centr-text {
            width: 900px;
            height: 260px;
            position: absolute;
            top: 50%;
            left: 50%;
            margin-top: -140px;
            margin-left: -450px;
        }
    </style>
</head>
<body>
<div class="centr-text">
    <p>
        <span style="color:#059528; font-size:200px; font-weight:400;">{{ error.code }}</span><br>
        {{ error.message }}<br>
        <span style="font-size:32px;">{{ error.hint }}</span>
    </p>
</div>

</body>
</html>